[project]
name = "tensor-viz-api"
version = "0.1.0"
description = "FastAPI + PyTorch tensor visualization API"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "numpy>=2.0.0",
    "pydantic>=2.9.0",
    "torch>=2.5.0",
]
